{% extends 'backend/admin_base.html.twig' %}

{% block title %} {{ parent() }} - {{ page_title }}{% endblock %}

{% block main %}

    <div class="row">
        <div class="col-12">
                <h1>Liste des métiers</h1>
                    <form action="{{ path('backend_job_add') }}" method="POST">
                        <button class="btn btn-success">
                            Ajouter un métier
                            <i class="fas fa-plus-circle"></i> 
                        </button>
                    </form>

            
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Name</th>
                                <th>actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {# Boucle #}
                            {% for job in jobs %}
                            <tr>
                                <td>
                                    {{ job.id }}
                                </td>
                                <td>
                                    <a href="#">
                                        {{ job.jobName }}
                                    </a>
                                </td>
                                <td>
                                    <a href="{{path('backend_job_show', {'id': job.id} )}}">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <form class="d-inline" action="{{path('backend_job_delete', {'id': job.id} )}}" method="POST">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <button class="text-danger bg-white border-0">&nbsp;<i class="far fa-trash-alt"></i></button>
                                    </form>
                                </td>
                            </tr>
                            {% endfor %}
                            {# Boucle #}
                        </tbody>
                    </table>

                    <form action="{{ path('backend_job_add') }}" method="POST">
                        <button class="btn btn-success">
                            Ajouter un métier
                            <i class="fas fa-plus-circle"></i> 
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    
{% endblock %}
